# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(semprTaskLib)
ADD_LIBRARY(${SEMPR_TASKLIB_NAME} SHARED 
    ${SEMPR_TASKLIB_SOURCES})
add_dependencies(${SEMPR_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${SEMPR_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${SEMPR_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SEMPR_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SEMPR_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${SEMPR_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${SEMPR_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SEMPR_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SEMPR_TASKLIB_HEADERS}
    DESTINATION include/orocos/sempr)

